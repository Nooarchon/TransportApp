<?xml version="1.0" encoding="UTF-8" ?>
<Shell 
x:Class="TransportApp.Mobile.AppShell" 
xmlns="http://schemas.microsoft.com/dotnet/2021/maui" 
xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
xmlns:pages="clr-namespace:TransportApp.Mobile.Pages" 
xmlns:sf="clr-namespace:Syncfusion.Maui.Toolkit.SegmentedControl;assembly=Syncfusion.Maui.Toolkit" 
Shell.FlyoutBehavior="Flyout" 
Title="Transport of Prague">

    <Shell.Resources>
        <ResourceDictionary>
            <FontImageSource x:Key="IconBus" Glyph="🚌" FontFamily="OpenSansRegular" Color="Gray" />
            <FontImageSource x:Key="IconRoute" Glyph="📍" FontFamily="OpenSansRegular" Color="Gray" />
            <FontImageSource x:Key="IconSettings" Glyph="⚙️" FontFamily="OpenSansRegular" Color="Gray" />
        </ResourceDictionary>
    </Shell.Resources>

    <!-- Home -->
    <FlyoutItem Title="Home" Icon="{StaticResource IconBus}">
        <ShellContent 
ContentTemplate="{DataTemplate pages:MainPage}" 
Route="main" />
    </FlyoutItem>

    <!-- Planner -->
    <FlyoutItem Title="Planner" Icon="{StaticResource IconRoute}">
        <ShellContent
ContentTemplate="{DataTemplate pages:RoutePlannerPage}"
Route="routePlanner" />
    </FlyoutItem>

    <!-- IMPORTANT:
The StopDetailPage route should NOT be here,
because it is registered via DI:
AddTransientWithShellRoute<StopDetailPage, StopDetailPageModel>("StopDetailPage")
-->

    <Shell.FlyoutFooter>
        <VerticalStackLayout Padding="20" Spacing="10">
            <Label Text="Application Theme" HorizontalOptions="Center" FontSize="12" TextColor="Gray"/>
            <sf:SfSegmentedControl x:Name="ThemeSegmentedControl"
SelectionChanged="SfSegmentedControl_SelectionChanged"
VerticalOptions="Center"
HorizontalOptions="Center">
                <sf:SfSegmentedControl.ItemsSource>
                    <x:Array Type="{x:Type x:String}">
                        <x:String>Light</x:String>
                        <x:String>Dark</x:String>
                    </x:Array>
                </sf:SfSegmentedControl.ItemsSource>
            </sf:SfSegmentedControl>
        </VerticalStackLayout>
    </Shell.FlyoutFooter>

</Shell>