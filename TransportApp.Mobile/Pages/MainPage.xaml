<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:TransportApp.Mobile.PageModels"             
             xmlns:models="clr-namespace:TransportApp.Mobile.Models"
             xmlns:pullToRefresh="clr-namespace:Syncfusion.Maui.Toolkit.PullToRefresh;assembly=Syncfusion.Maui.Toolkit"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="TransportApp.Mobile.Pages.MainPage"
             x:DataType="pageModels:MainPageModel"
             x:Name="OverviewPage"
             Title="{Binding Today}">

    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior
                EventName="Appearing"                
                Command="{Binding AppearingCommand}" />
    </ContentPage.Behaviors>

    <Grid RowDefinitions="Auto, *" Padding="20" RowSpacing="20">

        <Label Grid.Row="0" 
               Text="Ближайшее расписание" 
               FontSize="26"
               FontAttributes="Bold" />

        <pullToRefresh:SfPullToRefresh Grid.Row="1"
                                       IsRefreshing="{Binding IsRefreshing}"
                                       RefreshCommand="{Binding RefreshCommand}">

            <pullToRefresh:SfPullToRefresh.PullableContent>
                <CollectionView ItemsSource="{Binding Departures}" x:Name="DeparturesList">
                    <CollectionView.ItemTemplate>
                       <DataTemplate x:DataType="models:StopDeparture">
    <Frame Margin="0,5" Padding="15" CornerRadius="12" HasShadow="False"
           BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#252525}"
           BorderColor="{AppThemeBinding Light=#E0E0E0, Dark=#404040}">
        
        <Frame.GestureRecognizers>
            <TapGestureRecognizer 
                Command="{Binding BindingContext.GoToStopDetailsCommand, Source={x:Reference OverviewPage}}"
                CommandParameter="{Binding}" />
        </Frame.GestureRecognizers>

        <Grid ColumnDefinitions="50, *, Auto">
            <Label Grid.Column="0" Text="{Binding route_short_name}" 
                   FontAttributes="Bold" FontSize="18" VerticalOptions="Center"
                   TextColor="#512BD4"/> <Label Grid.Column="1" Text="{Binding trip_headsign}" 
                   FontSize="14" VerticalOptions="Center" Margin="10,0"
                   TextColor="{AppThemeBinding Light=Black, Dark=White}"/>

            <Label Grid.Column="2" Text="{Binding arrival_time}" 
                   FontAttributes="Bold" FontSize="16" VerticalOptions="Center"
                   TextColor="Gray"/>
        </Grid>
    </Frame>
</DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </pullToRefresh:SfPullToRefresh.PullableContent>
        </pullToRefresh:SfPullToRefresh>
    </Grid>
</ContentPage>